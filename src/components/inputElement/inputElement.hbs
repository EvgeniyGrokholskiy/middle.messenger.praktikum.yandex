<input
    {{#if disabled}}
    disabled
    {{/if}}
    required='{{required}}'
    id='{{for}}'
    name='{{name}}'
    value='{{value}}'
    type='{{type}}'
    placeholder='{{placeholder}}'
    class='{{class}} {{errorClass}}'
/>